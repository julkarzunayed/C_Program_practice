# include <stdio.h>
// Program for calculating the area of a square
int main() {
    float side;
    printf("Enter the value fo side: ");
    scanf("%f", &side);
    
    // float area = side * side;

    printf("The area is: %f", side * side);
    return 0;
}